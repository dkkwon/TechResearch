[제목] 대규모 C/C++ 프로젝트 내 미사용 구조체 필드 검출 방안 공유

안녕하세요,

프로젝트 내에서 메모리 할당 이후 추가 해제가 없고, memset으로 초기화는 수행되지만 실제 로직에서 읽히지 않는(Unused/Dead) 구조체 필드를 검출하기 위한 현실적인 방안을 정리하여 공유합니다.

규모가 큰 프로젝트 특성과 memset 초기화 방식을 고려한 최적의 접근법입니다.

---

1. 핵심 검출 도구 및 접근법

① Cppcheck (토큰 기반 분석)
- 특징: memset은 메모리 주소와 크기만 사용하므로 구조체 필드명을 직접 쓰지 않습니다. Cppcheck는 이를 인지하여 memset 초기화와 상관없이 실제 로직에서 참조(. 또는 ->)가 없는 필드를 'unusedStructMember'로 정확히 찾아냅니다.
- 실행 명령어:
  cppcheck --enable=style --quiet .

② Clang-Tidy + 컴파일 데이터베이스 (추천)
- 특징: 대규모 프로젝트의 수많은 #ifdef 분기와 복잡한 헤더 참조 관계를 완벽히 추적하기 위해 빌드 산출물인 'compile_commands.json'을 도구에 연동합니다.
- 활용 규칙: clang-analyzer-deadcode.DeadStores (값은 대입되었으나 소비되지 않는 필드 역추적)

③ 컴파일러 최적화 리포트 (빌드 중 산출물 활용)
- 특징: 컴파일러가 최적화 과정에서 자체적으로 제거한 데드 스토어(Dead Store) 리포트를 뱉어내게 하여 100% 정확한 미사용 필드 목록을 수집합니다.
- 옵션: 
  * GCC: -fopt-info-inline-missed 또는 -fopt-info-all
  * Clang: -Rpass=deadcode-elimination

④ Ctags + Grep 텍스트 카운팅
- 특징: memset으로만 지워지고 코드에서 안 쓰인 필드는 오직 헤더 선언부에만 존재합니다. 프로젝트 전체에서 해당 필드명의 출현 횟수가 '정확히 1회'인 항목을 스크립트로 전수조사하는 방식입니다.

---

2. 대규모 프로젝트 적용 시 주의사항 및 한계

- 100% 자동화의 한계: C/C++ 언어 특성상 포인터 주소 연산(Offset 접근), 구조체 통째로 외부 바이너리 라이브러리에 넘기는 로직 등이 있으면 도구가 데이터 흐름을 놓칠 수 있습니다.
- 현실적인 목표: 도구를 통해 전체의 약 80~90%를 자동으로 필터링한 후, 추출된 '우선 검토 대상 후보 목록'을 바탕으로 개발자가 최종 검토하는 프로세스를 권장합니다.

업무에 참고하시기 바랍니다.
감사합니다.
