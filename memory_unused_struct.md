초기화 코드에서 값을 일괄 할당(Write)하는 어플리케이션 특성상, 일반적인 정적 분석 도구는 초기화 시점의 접근을 "정상적인 사용"으로 판단합니다. 따라서 **"초기화 단계 이후 메인 런타임 루프에서 읽기/쓰기가 전혀 없는 메모리 영역(Dead Data)"**을 식별하기 위해서는 **OS 동적 추적, 구간 분리형 정적 분석, 하드웨어 PMU 프로파일링**을 결합해야 합니다.
해당 문제를 해결하기 위한 구체적인 4가지 접근 방안을 정리해 드립니다.
## 1. OS 레벨 추적: Linux Soft-Dirty Bit & 페이지 매핑
초기화 완료 시점과 런타임 시점을 동적으로 분리하여, OS가 관리하는 물리 페이지의 변경 이력을 추적하는 가장 확실한 실용적 방안입니다.
### 작동 메커니즘
 1. **초기화 완료 시점 동기화**: 어플리케이션이 Shared Memory 및 Huge Page 할당과 초기화를 모두 마친 직후(메인 루프 진입 전) 신호를 받습니다.
 2. **페이지 상태 리셋**: OS 커널 명령을 통해 해당 프로세스의 모든 가상 메모리 페이지의 Soft-Dirty 비트(페이지 변경 기록)를 0으로 리셋합니다.
 3. **런타임 시나리오 수행**: 주요 기능 및 형상별 런타임 테스트 시나리오(Runtime Phase)를 충분히 수행합니다.
 4. **미사용 페이지 추출**: 런타임이 끝난 후, 여전히 Soft-Dirty 비트가 0으로 남아있는(단 한 번도 쓰이거나 읽히지 않은) 메모리 페이지의 오프셋을 추출합니다.
 5. **심볼 역추적**: 미사용 물리 주소 영역을 ELF 심볼 테이블(DWARF 정보) 및 .map 파일과 매핑하여, 어떤 구조체 배열의 몇 번째 인덱스/필드가 방치되었는지 도출합니다.
### 관련 커널 인터페이스 및 분석 기능
 * **/proc/<PID>/clear_refs (옵션 4)**
   * **기능**: 프로세스의 Soft-Dirty 비트를 0으로 초기화합니다.
 * **/proc/<PID>/pagemap & /proc/<PID>/smaps**
   * **기능**: 각 가상 주소 페이지의 Soft-Dirty 상태, Huge Page 할당 여부, 물리 메모리 매핑 상태 정보를 시각화하고 읽어옵니다.
## 2. 정적 분석 확장: 구간 분리형(Phase-Aware) AST 분석
소스코드 수준에서 초기화 함수 내의 접근과 메인 런타임 로직 내의 접근을 분리하여 분석하는 정적 코드 분석 기법입니다.
### 작동 메커니즘
 1. **콜 그래프(Call Graph) 기반 구역(Phase) 정의**:
   * **Init Phase**: main() 진입 후 초기화 관련 함수군 (init_*(), setup_*()).
   * **Runtime Phase**: 메인 처리 루프 (main_loop(), event_process()) 및 하위 작업 함수군.
 2. **구조체 필드별 Access Count 트래킹**:
   * AST(추상 구문 트리) 상에서 모든 구조체 멤버 변수(MemberExpr) 및 배열 접근(ArraySubscriptExpr)의 위치를 추출합니다.
 3. **필터링 규칙 적용**:
   * Init Phase에서 발생하는 Read/Write는 접근 카운트에서 **제외**합니다.
   * Runtime Phase 구역 내부에서 수집된 Read/Write 카운트만 합산합니다.
 4. **결과 도출**: Runtime Access Count = 0인 구조체 필드 및 배열 인덱스를 **초기화 전용/런타임 미사용 메모리**로 규정합니다.
### 활용 도구 및 방식
 * **Clang AST Matcher 커스텀 분석기**: 소스코드 수정 없이 빌드 타임에 AST를 스캔하여 런타임 구역 내 참조 유무를 검증합니다.
 * **상용 정적 분석기(Coverity / PVS-Studio) 커스텀 규칙**: Call Graph 기반 Context-Sensitive 규칙을 정의하여 특정한 초기화 함수 내부에서의 변수 할당을 미사용 경고 대상에서 제외하지 않도록 설정합니다.
## 3. 하드웨어 PMU 기반 추적: Linux perf & PEBS
Shared Memory 영역에 대해 CPU 하드웨어 차원의 메모리 접근 샘플링을 수행하여 접근 빈도를 정밀 추적합니다.
### 작동 메커니즘
 1. **Intel PEBS (Precise Event-Based Sampling) / ARM SPE 활용**: CPU 하드웨어의 Performance Monitoring Unit(PMU)을 통해 특정 메모리 주소 범위(Shared Memory / Huge Page 영역)에 발생하는 Read/Write 로드·스토어 이벤트를 샘플링합니다.
 2. **Cold / Zero Access 영역 식별**: 런타임 수행 동안 메모리 주소별 접근 횟수 히트맵(Heatmap)을 작성하여, 메모리 주소 오프셋 중 **접근 횟수가 0인 구간**을 도출합니다.
### 관련 도구 및 주요 옵션
 * **perf mem (Linux Profiling Tool)**
   * **기능**: CPU 성능 카운터를 이용하여 메모리 Read/Write 접근 이벤트 및 발생 주소를 정밀 프로파일링합니다.
   * **주요 옵션**:
     * record -e mem-loads,mem-stores: 런타임 중 메모리 로드 및 스토어 이벤트만 선별 수집.
     * -p <PID>: 특정 어플리케이션 프로세스 타겟팅.
     * report --sort=mem,symbol: 접근 발생 메모리 주소 및 심볼별 정렬 출력.
## 4. 형상 매크로(#if) 검증 및 구조체 개선 연계
#if 조건부 컴파일 매크로로 인해 특정 빌드 형상에서는 유효하지만, 다른 형상에서는 초기화만 수행되고 런타임 로직이 비활성화되는 문제를 해결합니다.
### 분석 및 개선 절차
 1. **Multi-Configuration Matrix 스캔**:
   * 주요 매크로 조합(형상 Matrix)별로 빌드한 후, 상기 1~3번 기법을 자동 수행합니다.
   * "형상 A에서는 런타임에 쓰이지만, 형상 B에서는 초기화만 되고 방치되는 필드" 목록을 매핑합니다.
 2. **구조체 정의 레이아웃 개선**:
   * **조건부 필드 선언**: 해당 형상에서 런타임에 쓰이지 않는 필드는 구조체 정의 헤더에서도 동일한 #if 매크로로 감싸 메모리 할당 자체를 차단합니다.
   * **SoA (Structure of Arrays) 분리**: 구조체 배열 내에서 "초기화만 되는 필드(Cold Data)"와 "런타임에 자주 쓰이는 필드(Hot Data)"를 별도의 배열로 분리하여 Hot Data 배열만 Shared Memory / Huge Page 연속 공간에 밀집 배치합니다.





요청하신 조건(GitHub 복사 관련 내용 제거, 실제 실행 코드 제거, 도구별 기능 및 옵션 명시, 적합한 항목으로 전체 내용 재구성)을 반영하여 정리한 최적화 가이드 문서입니다.
# C/C++ 어플리케이션 SW 메모리 최적화 가이드
## 1. 개요 및 시스템 특성
본 가이드는 초기화 시점에 대부분의 메모리를 정적 할당하고 런타임 동적 할당을 극도로 지양하는 C/C++ 어플리케이션 SW의 메모리 최적화 방안을 다룹니다.
 * **시스템 환경**: 초기화(Init Phase) 시 Shared Memory 및 Huge Page에 대용량 정적/일괄 할당.
 * **개발 특성**: #if 매크로 기반의 다양한 형상(Configuration) 및 기능 조건부 컴파일 사용.
 * **주요 최적화 과제**:
   * 구조체 내부 패딩(Padding)으로 인한 L1/L2/L3 캐시 미스 및 Huge Page 단편화 방지.
   * #if 형상별 미사용/예약(Reserved) 필드 및 과도하게 큰 정적 배열 식별.
   * 초기화 코드에서는 값이 작성되지만 메인 런타임 진입 후에는 읽기/쓰기가 없는 Dead Data 감지.
## 2. 정적 분석 도구 및 기능 정리 (도구별 옵션 포함)
### ① 구조체 패딩 및 정렬 분석 도구
 * **pahole (Poke-a-hole)**
   * **주요 기능**: DWARF 디버깅 정보를 분석하여 구조체 내 패딩, Hole 위치, 64바이트 캐시라인 경계 침범 여부를 시각화하고 최적의 필드 순서를 제안합니다.
   * **주요 옵션**:
     * -C: 분석할 특정 구조체명 지정.
     * --reorganize: 패딩을 최소화하도록 구조체 필드 재배치안 생성.
     * --show_reorg_steps: 재배치 과정 및 절감되는 메모리 크기 단계별 출력.
 * **컴파일러 패딩 경고 (-Wpadded)**
   * **주요 기능**: GCC/Clang 컴파일 시 메모리 정렬을 위해 컴파일러가 임의로 패딩을 삽입한 모든 구조체 위치와 바이트 크기를 경고로 출력합니다.
 * **Clang-Tidy**
   * **주요 기능**: 정적 코드 분석을 통해 구조체 필드 재배치 시 절약 가능한 메모리 용량을 리포팅합니다.
   * **주요 체커**: optin.performance.Padding
### ② 미사용 필드 및 정적 섹션 분석 도구
 * **상용/오픈소스 정적 분석기 (PVS-Studio, Coverity, Cppcheck)**
   * **주요 기능**: 전체 소스코드의 AST(구문 트리)를 스캔하여 선언 후 코드 전체에서 참조가 전혀 없는 멤버 변수 및 미사용 필드를 감지합니다.
   * **주요 체커/검사 항목**: PVS-Studio V730 계열 검사, Coverity UNUSED_VALUE / UNREAD_VARIABLE.
 * **Clang AST Matcher (커스텀 체커)**
   * **주요 기능**: 분석 구간을 분리하여 초기화 함수 내부의 접근은 제외하고, 메인 런타임 루프 진입 이후의 참조 횟수만 카운팅하여 런타임 미사용 필드를 식별합니다.
 * **Google Bloaty McBloatface (bloaty)**
   * **주요 기능**: ELF 바이너리의 .bss 및 .data 섹션 심볼 크기를 정렬하여 정적 메모리를 가장 많이 점유하는 구조체 및 배열을 도출합니다.
   * **주요 옵션**:
     * -d symbols: 심볼 단위 분석 프로파일링 지정.
     * -n <숫자>: 상위 N개 대형 심볼만 출력.
     * -s vm: Virtual Memory 크기 기준으로 정렬.
 * **nm / readelf (GNU Binary Utilities)**
   * **주요 기능**: 실행 파일 및 라이브러리 내 전역 변수, Shared Memory 매핑 심볼의 크기를 정렬하여 과도하게 크게 잡힌 정적 배열을 식별합니다.
   * **주요 옵션**:
     * --print-size: 심볼의 크기 함께 출력.
     * --size-sort: 심볼 크기 순으로 정렬.
     * --radix=d: 심볼 크기 및 주소를 10진수로 표기.
## 3. 런타임 미사용 메모리 추적 기법 (Phase-Aware & OS Tracking)
초기화 구문에서 메모리를 일괄 작성하기 때문에 일반 정적 분석기로는 Dead Data를 찾기 어렵습니다. 다음 기법을 병행합니다.
 * **Linux Soft-Dirty Bit 기반 런타임 추적**
   * **개념**: Linux 커널 기능을 이용해 초기화 단계 완료 직후 메모리 변경 이력(Dirty Bit)을 리셋하고, 런타임 시나리오 동작 후에도 터치되지 않은 물리 페이지 오프셋을 역추적합니다.
   * **분석 절차**: 어플리케이션 초기화 완료 시점에 OS 매핑 비트 리셋 → 메인 런타임 시나리오 수행 → OS의 메모리 맵 정보를 통해 미사용 물리 페이지 식별 → DWARF 심볼 매핑을 통한 최종 미사용 필드/인덱스 도출.
 * **Multi-Configuration Matrix (#if 형상 검증)**
   * **개념**: 주요 매크로 조합별로 자동화 빌드 분석을 수행하여, 특정 형상에서만 유효하고 다른 형상에서는 초기화만 된 채 방치되는 필드를 매핑합니다.
## 4. Shared Memory & Huge Page 특화 검증 방안
 * **캐시라인(64 Bytes) 경계 정렬 및 False Sharing 방지**
   * Frequent Read/Write 필드가 캐시라인 경계에 걸쳐 있으면 Cache Miss가 증가합니다. Critical Path 구조체에 64바이트 정렬 키워드(alignas(64))를 적용하여 패딩 및 경계를 관리합니다.
 * **Huge Page (2MB/1GB) Boundary Off-by-one 검증**
   * Shared Memory 매핑 구조체의 전체 크기가 2MB 경계를 미세하게 초과할 경우 OS는 2MB 페이지를 추가 할당하여 수 MB의 메모리가 낭비됩니다. 컴파일 타임 단증 구문(static_assert)을 추가하여 구조체 크기가 경계를 넘지 않도록 정적 검증을 수행합니다.
## 5. 코드 구조 개선 패턴 (Refactoring Patterns)
 * **조건부 구조체 패킹 (Conditional Struct Packing)**
   * 로직에 적용된 #if 형상 매크로를 구조체 정의 헤더 파일에도 동일하게 적용하여, 해당 형상 미사용 시 필드 자체가 메모리를 차지하지 않도록 개선합니다.
 * **SoA (Structure of Arrays) 구조 전환**
   * 기존 배열 내에 구조체가 들어가는 구조(AoS)에서 런타임 사용 빈도가 매우 낮거나 초기화만 되는 필드를 별도 배열로 분리(SoA)합니다. 이를 통해 자주 접근하는 핵심 데이터의 L1/L2 캐시 밀도를 극대화합니다.
